{
  "name": "plato",
  "version": "0.12.6",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --env-file=.env bin/server.js",
    "dev": "node --env-file=.env --env-file=.env.dev bin/server.js",
    "migrate": "node --env-file=.env bin/migrate.js",
    "test": "node --test 'test/**/*.test.js'"
  },
  "dependencies": {
    "@dicebear/collection": "^9.4.2",
    "@dicebear/core": "^9.4.2",
    "knowless": "^1.1.5",
    "marked": "^18.0.2",
    "unique-names-generator": "^4.7.1"
  }
}
